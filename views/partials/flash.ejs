<% if(success && success.length) {%>

    
      <script>
        let success = '<%= success%>';
      swal("Disaster", success, "success");
         
        </script>
       

   
    <% } %>
    
    <% if(error && error.length) {%>
      <script>
      let alert = '<%= error%>';
    swal("Disaster", alert, "warning");
       
      </script>
        
    
    <% } %>